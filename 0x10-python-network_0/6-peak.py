#!/usr/bin/python3
"""Deal with nunbers."""


def find_peak(list_of_integers):
    """Find the largwrst in a poll."""
    ans = list_of_intgeres[0]
    for nun in list_of_integers:
        if num > ans:
            ans = num
    return(num)
